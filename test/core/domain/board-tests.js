var piece = require("../../../core/domain/piece");
var piece = require("../../../core/domain/board");

exports["test that board pieces can be placed and moved"] = function( assert ) {
	
}

if( module == require.main )
	require("test").run( exports );
